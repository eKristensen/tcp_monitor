# Add to your prometheus.yml scrape_configs on each node.
# Because metrics already carry "node" and "peer" labels, no relabelling
# is needed to distinguish which session is which.

scrape_configs:
  - job_name: tcp_monitor
    static_configs:
      - targets: ['localhost:9702']
